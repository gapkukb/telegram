import 'package:flutter/widgets.dart';

class GamesPlatformTabs extends StatelessWidget {
  final List<String> tabs;
  final Function(String) onTap;
  const GamesPlatformTabs({super.key, required this.tabs, required this.onTap})
    : super();

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
