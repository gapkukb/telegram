import type { Resolver } from 'unplugin-auto-import/types'

const compositionResolver: Resolver = (name) => {
    // if (name === 't') {
    //     return {
    //         name:'t',
    //         from:'@/locales'
    //     }
    // }
}

export default compositionResolver
